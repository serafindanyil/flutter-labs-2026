name: shake_flashlight_plugin
description: Secret Android flashlight toggle plugin.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.1
  flutter: ">=3.41.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.serafindanyil.shake_flashlight_plugin
        pluginClass: ShakeFlashlightPlugin
